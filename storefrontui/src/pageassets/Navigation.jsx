import React, { Component } from 'react';

class Navigation extends Component {
  render() {
    return (
      <nav>
        <a href="#">Home</a>
        <a href="#">Browse</a>
        <a href="#">Contact</a>
      </nav>
    );
  }
}

export default Navigation;